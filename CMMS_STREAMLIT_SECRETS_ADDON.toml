# Add to the existing UAT Streamlit Secrets. Keep the existing SUPABASE_* and DB_TABLE values.
CMMS_QAV_DUE_DATE_PASSWORD = "replace-with-a-new-cmms-qav-password"
CMMS_EXTENSION_REQUESTS_TABLE = "cmms_due_date_extension_requests"

# Optional overrides. Add these only if CMMS uses a different UAT table or Storage bucket.
# CMMS_DB_TABLE = "your_cmms_uat_issue_table"
# CMMS_STORAGE_BUCKET = "your_cmms_uat_storage_bucket"

# Optional server-only credentials. Use real ASCII Supabase keys only, never the sample values below.
# CMMS_SUPABASE_SERVICE_ROLE_KEY = "your-supabase-service-role-key"
# CMMS_TEAMS_QAV_WEBHOOK = "https://your-teams-webhook"
# CMMS_EXCEL_UPDATE_WEBHOOK = "https://your-power-automate-webhook"
